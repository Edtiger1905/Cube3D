#include "cube3d.h"

// tmp = ft_strtrim(line, "\n");
// if (is_map(tmp))
//     start_read_map = 1;
// if (!is_map(tmp) && start_read_map == 1)
// {
//     free(tmp);
//     free(line);
//     perror_and_exit(cube3d, "ciao");
// }
// printf("%s\n", tmp);
// free(tmp);
// free(line);